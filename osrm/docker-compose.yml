services:
  foursquaretky-car:
    image: osrm/osrm-backend
    command: osrm-routed --algorithm mld --max-table-size 250 /data/region.osrm
    volumes:
      - ./data/foursquaretky/car:/data
    ports:
      - "5000:5000"
    restart: unless-stopped

  foursquaretky-foot:
    image: osrm/osrm-backend
    command: osrm-routed --algorithm mld --max-table-size 250 /data/region.osrm
    volumes:
      - ./data/foursquaretky/foot:/data
    ports:
      - "5001:5000"
    restart: unless-stopped

  yelp-car:
    image: osrm/osrm-backend
    command: osrm-routed --algorithm mld --max-table-size 250 /data/region.osrm
    volumes:
      - ./data/yelp/car:/data
    ports:
      - "5002:5000"
    restart: unless-stopped

  yelp-foot:
    image: osrm/osrm-backend
    command: osrm-routed --algorithm mld --max-table-size 250 /data/region.osrm
    volumes:
      - ./data/yelp/foot:/data
    ports:
      - "5003:5000"
    restart: unless-stopped
